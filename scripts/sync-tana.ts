/**
 * Sync script to export Tana content to JSON
 * Run with: npx ts-node scripts/sync-tana.ts
 *
 * This is a placeholder - the actual sync happens via Claude Code
 * using the Tana MCP tools. Run `claude` and ask it to sync your content.
 */

console.log(`
To sync your Tana content to the portfolio:

1. Run: claude
2. Say: "Sync my Tana content to the portfolio"

Claude will use the Tana MCP tools to fetch your #my-content nodes
and write them to data/content.json
`);
